# ============================================================
# AMOSKYS v0.9.1-beta — Production Dependencies
# ============================================================
# This file mirrors pyproject.toml [dependencies] + [enrichment]
# + [web] + [ml] for a complete production install.
#
# Install:  pip install -r requirements.txt
# Dev:      pip install -r requirements.txt -r requirements-dev.txt
# Or:       pip install -e ".[all]"
# ============================================================

# ── Core Protocol & Messaging (EventBus myelin) ──
grpcio>=1.70.0,<2.0
grpcio-tools>=1.70.0,<2.0
protobuf>=6.31.0,<7.0

# ── Configuration & Serialization ──
pyyaml>=6.0,<7.0
pydantic>=2.10.0,<3.0
python-dotenv>=1.1.0,<2.0

# ── Security & Cryptography (proof chain) ──
cryptography>=44.0.0,<45.0
pycryptodome>=3.21.0,<4.0
bcrypt>=4.3.0,<5.0
PyJWT>=2.10.0,<3.0

# ── Monitoring & System Introspection ──
psutil>=6.1.0,<7.0
prometheus-client>=0.21.0,<1.0

# ── Networking (agent blood supply) ──
requests>=2.32.0,<3.0
aiohttp>=3.11.0,<4.0
dnspython>=2.8.0,<3.0
urllib3>=2.5.0,<3.0

# ── Detection Engine (sigma muscle fibers) ──
lark>=1.3.0,<2.0

# ── CLI & Display ──
click>=8.1.0,<9.0
rich>=13.9.0,<14.0

# ── Enrichment (sensory integration — GeoIP/ASN) ──
maxminddb>=2.6.0,<3.0

# ── Web Dashboard (consciousness layer) ──
Flask>=3.1.0,<4.0
Flask-SocketIO>=5.3.0,<6.0
Werkzeug>=3.1.0,<4.0
gunicorn>=21.2.0,<23.0
eventlet>=0.36.0,<1.0
simple-websocket>=1.1.0,<2.0
Jinja2>=3.1.0,<4.0
MarkupSafe>=3.0.0,<4.0
itsdangerous>=2.2.0,<3.0
websocket-client>=1.8.0,<2.0
sqlalchemy>=2.0.0,<3.0
argon2-cffi>=23.1.0,<24.0
flask-limiter>=3.5.0,<4.0
flask-wtf>=1.2.0,<2.0
flask-talisman>=1.1.0,<2.0

# ── ML / Intelligence (SOMA brain) ──
pandas>=2.0.0,<3.0
numpy>=1.24.0,<3.0
scikit-learn>=1.3.0,<2.0
joblib>=1.4.0,<2.0
