# ============================================================
# AMOSKYS v0.9.1-beta — Development Dependencies
# ============================================================
# Install: pip install -r requirements.txt -r requirements-dev.txt
# Or:      pip install -e ".[all]"
# ============================================================

# ── Testing ──
pytest>=8.4.0,<9.0
pytest-asyncio>=0.24.0,<1.0
pytest-cov>=6.0.0,<7.0
coverage>=7.0.0,<8.0

# ── Code Quality (pinned for CI consistency) ──
black==24.10.0
isort==5.13.2
flake8>=7.1.0,<8.0
ruff>=0.8.0,<1.0

# ── Type Checking ──
mypy>=1.13.0,<2.0

# ── CI Security Scanning ──
safety>=3.0.0
bandit>=1.7.5,<2.0

# ── Pre-commit ──
pre-commit>=3.6.0,<4.0
