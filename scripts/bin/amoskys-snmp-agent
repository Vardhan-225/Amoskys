#!/usr/bin/env python3
"""
AMOSKYS SNMP Agent - Production Entry Point
Collects SNMP telemetry and publishes to EventBus
"""

import sys
from pathlib import Path

# Add src to path
sys.path.insert(0, str(Path(__file__).parent / "src"))

# No macOS Observatory equivalent for SNMP agent
# from amoskys.agents.shared.snmp.agent import main
print("SNMP Agent: no macOS Observatory equivalent available", file=sys.stderr)
sys.exit(1)
