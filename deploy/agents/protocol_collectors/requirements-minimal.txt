# Minimal runtime dependencies for ProtocolCollectorsV2 agent
# No heavy ML/TensorFlow dependencies - just core protocol detection

protobuf>=4.21.0
psutil>=5.9.0
pydantic>=2.0.0
python-dotenv>=0.19.0
prometheus_client>=0.12.0

# Optionally: if you need pcap/packet capture
# scapy>=2.5.0
# dpkt>=1.9.8
